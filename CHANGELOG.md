# Changelog

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，
并且遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### 变更

- 将三个邮件查询工具收敛为 `email_assistant_query_mailbox`，通过 `operation=list`、`latest` 或 `read` 区分邮件头列表、最新邮件和按 UID 阅读。
- 将邮件总结与翻译工具收敛为 `email_assistant_process_message`，通过 `task=summary` 或 `translate` 区分处理类型。
- 确认发送工具仅在当前消息匹配一次性确认格式时注入；取消草稿工具仅在当前用户存在可取消 Bot 草稿时注入。

### 兼容性

- 已配置人格工具白名单时，将 `email_assistant_list_messages`、`email_assistant_get_latest_message`、`email_assistant_show_message` 替换为 `email_assistant_query_mailbox`；将 `email_assistant_summarize_message`、`email_assistant_translate_message` 替换为 `email_assistant_process_message`。

## [2.4.1] - 2026-08-29

### 变更

- 重构邮件中心 WebUI，采用 Quiet Glass 视觉系统，统一浅色/深色主题、表面层级、语义状态、表单和反馈样式。
- 桌面端保留邮件列表与详情双栏工作台，并优化账户、文件夹、搜索、草稿和邮件操作的密度与可读性。
- 手机端新增可关闭的抽屉导航；账户、文件夹和新建文件夹入口在窄屏下保持可达。
- 手机端打开邮件时切换为单焦点详情视图，提供返回列表按钮，避免在列表下方反复滚动查找正文。
- 窄屏下重新排列搜索、日期筛选、草稿编辑器和操作按钮，支持 320px 及以上视口而不产生页面级横向滚动。

### 可访问性

- 确认、建文件夹和 AI 处理弹层支持 Escape、遮罩关闭和关闭后的焦点回归。
- 移动、发送和删除操作使用危险语义样式；弹层打开时锁定背景滚动并保留明确的确认文案。
- 为导航抽屉、弹层和移动详情返回操作补充可访问名称与状态属性。

### 测试

- 完整 Python 测试集共 105 项通过。
- 使用 Playwright 检查浅色/深色桌面、390px 手机、320px 窄屏、移动导航、邮件详情和危险操作确认弹层。

## [2.4.0] - 2026-07-27

### 新增

- **稳定邮件实体**：将邮件身份与 IMAP 文件夹位置拆分，优先使用 Gmail `X-GM-MSGID`，其次使用规范化 `Message-ID`，缺失时回退邮件头指纹。
- **数据库版本控制**：新增 `db_version` 表和 schema 版本管理器，当前数据库版本为 `v1`，为后续数据库迁移提供明确基线。
- **跨文件夹缓存复用**：同一账户内移动或复制邮件后，新文件夹 UID 可继续使用原有正文、总结和翻译缓存。

### 变更

- 正文和 AI 结果缓存改为关联稳定邮件实体，不再直接绑定 `folder + UIDVALIDITY + UID`。
- 邮件移出文件夹后提供 24 小时清理宽限期；仅在没有其他有效位置时按配置清理正文和 AI 缓存。
- 文件夹 UIDVALIDITY 变化后，旧 UID 仍会失效；重新索引出的邮件若能识别为同一实体，则继续复用已有缓存。
- 插件版本更新为 `v2.4.0`。

### 修复

- 修复云端移动邮件后，本地已缓存正文、总结和翻译无法随新 UID 继续使用的问题。
- 邮件头内容或正文内容确认变化时，清理对应的旧正文和 AI 处理结果，避免显示过期缓存。
- 后续云端核对即使没有新的状态变化，也会继续执行到期缓存清理，确保真正删除的邮件最终被回收。

### 兼容性

- 本次不迁移此前未受版本管理的开发数据库。检测到无 `db_version` 表的旧 `mail_headers.db` 时，本地索引会停用并回退实时 IMAP；删除旧数据库并重启插件后会从云端重建。

### 测试

- 完整测试集共 105 项，覆盖跨文件夹正文/AI 缓存复用、Gmail 稳定消息 ID、内容变化失效、数据库版本记录与不兼容数据库拒绝。

## [2.3.1] - 2026-07-19

### 优化

- 精简邮件 LLM 工具名称、描述和参数 schema，降低每轮对话中工具定义占用的 Token。
- 合并重复查询路径，引导模型优先使用一次调用即可完成任务的工具，减少中间步骤消息和无效工具调用。
- 根据账户能力动态过滤只读、处理和写信工具，不向模型暴露当前会话无法使用的工具。

## [2.3.0] - 2026-07-17

### 新增

- 新增 Bot 可调用的邮件总结和翻译工具。
- 工具优先读取最新 AI 缓存；缓存不存在时自动校验云端邮件、读取正文、调用无人格模型并写回缓存。
- Bot 工具读取的新正文复用按需正文缓存，无需在主 Agent 上下文中传递完整邮件正文。

## [2.2.5] - 2026-07-17

### 新增

- 邮件中心支持打开邮件时自动显示已有总结或翻译缓存。
- 总结和翻译按钮新增强制重新处理入口，可临时指定目标语言。
- 配置项按账户、通知与 LLM、总结与翻译、WebUI、存储等相关板块重新组织。

### 优化

- 已缓存正文可立即显示，并在后台执行云端校验。
- 合并同一邮件的并发正文请求，并增加 WebUI 云端校验冷却，减少频繁切换邮件产生的重复 IMAP 请求。
- 每封邮件的总结和翻译各只保留最新一份结果，普通点击和自动显示不再按默认语言误判缓存。

### 修复

- 修复正文拉取成功后当前页面状态未更新、再次打开仍重复拉取的问题。
- 修复已缓存总结和翻译仍等待远程正文路径导致响应缓慢的问题。
- 修复重新总结或翻译后仍返回旧语言缓存，以及 Markdown 强调标记与链接显示异常的问题。

## [2.1.0] - 2026-07-17

### 新增

- 新增 LLM 安全写信工具：创建新邮件草稿、创建回复草稿、确认发送和取消草稿。
- 发送采用“生成草稿 -> 用户下一轮明确确认 -> SMTP 发送”流程；确认码短时有效、单次使用，并在发送前再次校验账户权限。
- 总结和翻译结果使用本地固定版本 `markdown-it` 渲染，并由 DOMPurify 清理后显示。
- 增加插件仓库地址和插件图标。

### 安全

- 草稿发送使用原子状态，SMTP 结果不确定时不会自动重试，避免重复发信。
- Markdown 渲染不使用远程 CDN，原始 HTML 默认关闭，并在插入页面前执行清理。

## [2.0.0] - 2026-07-16

### 新增

- 新增 AstrBot Plugin Pages 邮件中心，可从插件详情侧边栏进入。
- WebUI 支持账户和文件夹浏览、邮件列表、正文详情、草稿审核与发送。
- 支持 IMAP 文件夹清单、创建文件夹以及复制和移动邮件；移动优先使用 `UID MOVE`，仅在安全条件满足时回退。
- 新增按需正文缓存、缓存容量和保留期限管理，以及独立的本地草稿数据层。
- WebUI 邮件详情支持无人格模型总结和翻译，并缓存处理结果。

## [1.6.0] - 2026-07-16

### 新增

- 使用 `plugin_data` 中的 SQLite 建立持久化邮件头索引。
- 首次优先索引近期邮件，后续轮询逐步回填完整历史邮件头。
- 支持云端 UID 核对、删除状态标记、UIDVALIDITY 代际隔离和重启后续建索引。
- 新增 `/email sync`，列表和详情查询优先使用本地索引。

## [1.4.0] - 2026-07-16

### 新增

- 新增只读 LLM 邮件工具，可查询可用账户、邮件列表、最新邮件和指定 UID 邮件详情。
- 普通用户只能访问绑定账户，管理员仍需在私聊中操作；工具遵循账户查询能力开关。

### 优化

- “最新一封邮件”可在一次工具调用内完成定位和正文读取，减少模型逐步查询产生的中间对话。
- 标题通知增加账户 ID 和邮件 UID，方便用户继续通过命令或 LLM 查询。

## [1.3.0] - 2026-07-16

### 新增

- 新邮件通知新增标题直发、直接 LLM 转述和 AstrBot 官方定时任务转述三种模式。
- 直接 LLM 转述使用当前会话人格，可配置是否写入 AstrBot 官方对话历史。
- 支持自定义转述提示词和指定转述 Provider；留空时回退 AstrBot 当前模型。

## [1.0.0] - 2026-07-15

### 新增

- 首个可用版本，支持多邮箱 IMAP 自动收信、手动检查、邮件列表和详情查询。
- 支持 SMTP SSL/STARTTLS 纯文本发送，以及带 `In-Reply-To` 和 `References` 的线程回复。
- 使用 `/email` 命令组集中提供状态、测试、检查、查询、发送和回复命令。
- 每个邮箱可独立配置启用状态、绑定私聊用户，以及接收、查询和发送权限。
- 支持每账户独立 HTTP CONNECT、SOCKS4 和 SOCKS5 网络代理。
- 首次启用仅建立最大 UID 基线，不推送历史邮件；后台与手动检查使用账户锁避免重复通知。
